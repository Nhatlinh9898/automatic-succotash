# ==========================================
# 3D MC Presenter PRO - Quick Setup Guide
# ==========================================
# 
# Hướng dẫn cài đặt file .env đầy đủ
# 
# 1. Copy file này thành .env:
#    cp .env.setup .env
#
# 2. Điền các API key của bạn
# 3. Chạy: npm run dev:full
#
# ==========================================

# AI PROVIDER SELECTION
# Options: gemini, openai, claude
# Gemini: Free tier available, good for Vietnamese
# OpenAI: GPT-3.5/4, high quality but paid
# Claude: Anthropic, good reasoning
AI_PROVIDER=gemini

# ==========================================
# GOOGLE GEMINI API (RECOMMENDED)
# ==========================================
# 1. Truy cập: https://aistudio.google.com/
# 2. Đăng nhập bằng Google account
# 3. Click "Get API Key"
# 4. Copy key và dán vào dưới
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-pro
GEMINI_TEMPERATURE=0.7
GEMINI_MAX_TOKENS=1000

# ==========================================
# OPENAI GPT API (ALTERNATIVE)
# ==========================================
# 1. Truy cập: https://platform.openai.com/
# 2. Đăng nhập và tạo API key
# 3. Nạp tiền vào account (minimum $5)
# 4. Copy key và dán vào dưới
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=1000

# ==========================================
# GOOGLE TTS API (REQUIRED FOR VOICE)
# ==========================================
# 1. Truy cập: https://console.cloud.google.com/
# 2. Tạo project mới
# 3. Enable "Cloud Text-to-Speech API"
# 4. Tạo Service Account Key
# 5. Copy JSON key content hoặc chỉ cần API key
TTS_API_KEY=your_google_tts_api_key_here
TTS_LANGUAGE_CODE=vi-VN
TTS_VOICE_NAME=vi-VN-Wavenet-A
TTS_ENCODING=MP3

# Voice mapping cho từng nhân vật
TTS_VOICE_FEMALE1=vi-VN-Wavenet-A  # Linh - Professional
TTS_VOICE_FEMALE2=vi-VN-Wavenet-B  # Mai - Creative
TTS_VOICE_FEMALE3=vi-VN-Wavenet-C  # An - Academic
TTS_VOICE_FEMALE4=vi-VN-Wavenet-D  # Huong - Elegant
TTS_VOICE_FEMALE5=vi-VN-Wavenet-E  # Giang - Tech
TTS_VOICE_FEMALE6=vi-VN-Wavenet-F  # Trang - Energetic

# ==========================================
# SERVER CONFIGURATION
# ==========================================
PORT=3001
NODE_ENV=development
HOST=localhost

# CORS cho React frontend
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true

# ==========================================
# BASIC SETTINGS (KHÔNG CẦN THAY ĐỔI)
# ==========================================
# Default character khi khởi động
DEFAULT_CHARACTER=female1
DEFAULT_OUTFIT=casual
DEFAULT_HAIR=long
DEFAULT_ACCESSORY=none
DEFAULT_COLOR=#ff6b6b

# Enable features
ENABLE_AI_RESPONSES=true
ENABLE_TTS=true
ENABLE_LIP_SYNC=true
ENABLE_CHARACTER_SWITCHING=true
ENABLE_OUTFIT_CUSTOMIZATION=true
ENABLE_CAMERA_CONTROLS=true

# OBS Stream settings
OBS_WIDTH=1920
OBS_HEIGHT=1080
OBS_FPS=30

# ==========================================
# DEBUG (OPTIONAL)
# ==========================================
# Set to true để xem detailed logs
DEBUG=false
DEBUG_THREEJS=false
DEBUG_AI=false
DEBUG_TTS=false

# ==========================================
# HƯỚNG DẪN NHANH
# ==========================================
#
# BƯỚC 1: Lấy API Keys
# - Gemini: https://aistudio.google.com/ (Free)
# - Google TTS: https://console.cloud.google.com/ (Free tier)
#
# BƯỚC 2: Cài đặt
# cp .env.setup .env
# # Edit .env với API keys của bạn
#
# BƯỚC 3: Chạy
# npm install
# npm run dev:full
#
# BƯỚC 4: Mở trình duyệt
# http://localhost:3000 (React version)
# http://localhost:3001 (Server info)
#
# ==========================================
# TROUBLESHOOTING
# ==========================================
#
# LỖI "API key invalid":
# - Kiểm tra lại API key có đúng không
# - Đảm bảo API được enabled trong console
#
# LỖI "TTS not working":
# - Kiểm tra Google Cloud billing
# - Enable Text-to-Speech API
#
# LỖI "Cannot connect to backend":
# - Kiểm tra port 3001 có trống không
# - Chạy npm run ai-server trước
#
# ==========================================
